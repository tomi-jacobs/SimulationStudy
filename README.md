Bash loop.

These are the commands run for the simulation study of the Aizoaceae project. 

Note: the reason for the sed command is there's a bug in seq-gen that doesn't allow it to take in trees with support values
